On-chart panel (table) showing: 
  selected zone, 
  approach state, 
  lifecycle, 
  touchPressure, 
  priors + live posteriors

Validated-zones toggle (both display and optional selection)

TODO Concurrency with per-zone episode structs.
